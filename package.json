{
  "name": "fake-product",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm run build --prefix client && npm start --prefix server",
    "build": "CI=false && npm run build --prefix client",
    "install-both": "CI=false && npm install --prefix client --legacy-peer-deps && npm install --prefix server"
  },
  "author": "",
  "license": "ISC"
}
