{
  "name": "fake-product",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm run build --prefix client && npm start --prefix server",
    "build": "npm run build --prefix client",
    "install-both": "npm install --prefix client && npm install --prefix server"
  },
  "author": "",
  "license": "ISC"
}
